<div class="persons">
    {% for item in items %}
    <div class="person">
        <img src="{{ item.foto_value.value }}"> <h3>{{ item.name }}</h3>
        <p>{{ item.description }}</p>
    </div>
    {% endfor %}

</div>